@model List<RenderWidgetModel>
@foreach (var widget in Model)
{
    @await Component.InvokeAsync(widget.WidgetViewComponentName, new {widgetZone = widget.WidgetZone, additionalData = widget.AdditionalData});
}